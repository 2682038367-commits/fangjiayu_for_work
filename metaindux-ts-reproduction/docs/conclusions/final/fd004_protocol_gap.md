# 历史 FD004 “协议层”分析：已撤回

> **不要引用本文件旧结论。** 旧分析错误地将本地真实数据训练基线与论文“生成数据训练”
> 点估计之差解释为协议差异。论文未公开真实数据训练基线，所以该协议差异、论文生成
> 数据惩罚以及相关显著性检验均不可识别。

FD004 目前可报告的结果为：论文生成数据 RMSE 点估计 26.577；本地生成数据训练 RMSE
为 33.277；本地真实数据训练 RMSE 为 27.906。因此：

- 正式复现差距为 `33.277 − 26.577 = +6.700`；
- 本地生成数据惩罚为 `33.277 − 27.906 = +5.370`。

这不是论文侧的生成惩罚或协议层。请以 `results/frozen_numbers.json` 的
`observable_comparisons` 和 `analysis/diagnostics/protocol_gap_decomposition.py` 为准；
本路径只保留用于兼容旧链接。
